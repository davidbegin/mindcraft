services:
  mindcraft:
    build: .
    environment:
      SETTINGS_JSON: |
        {"auto_open_ui": false}
    volumes:
      - ./settings.js:/app/settings.js
      - ./keys.json:/app/keys.json
      - ./profiles:/app/profiles
      - ./bots:/app/bots
    ports:
      # Localhost only — do not publish on the LAN.
      - "127.0.0.1:3000-3003:3000-3003" # bot camera views: http://localhost:3000/
      - "127.0.0.1:8080:8080" # Mindserver port
    extra_hosts:
    - "host.docker.internal:host-gateway"

  viaproxy: #use this service to connect to an unsupported minecraft server versions. more info: ./services/viaproxy/README.md
    image: ghcr.io/viaversion/viaproxy:latest
    volumes:
      - ./services/viaproxy:/app/run
    ports:
      - "127.0.0.1:25568:25568"
    profiles:
      - viaproxy
    stdin_open: true
    tty: true
